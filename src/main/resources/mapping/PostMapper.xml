<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lsun.bbs171.repository.PostRepository">

    <insert id="submit" parameterType="Post">
        insert into posts(phone, title, content, category)
        values (#{phone}, #{title}, #{content}, #{category})
    </insert>

    <select id="findPostsForIndex" resultType="PostsForIndex">
        select posts.id,
        posts.title,
        posts.datetime,
        posts.category,
        posts.views,
        posts.comment,
        users.id user_id,
        users.username,
        users.avatar
        from posts
        inner join users on users.phone = posts.phone and posts.status = 1
        <if test="category != 0">
            and category = #{category}
        </if>
        <if test="keywords != ''">
            and title REGEXP #{keywords}
        </if>
        order by ${sort} desc
    </select>

    <select id="findPostDetail" resultType="PostDetail">
        select posts.*,
               users.id user_id,
               users.role,
               users.username,
               users.gender,
               users.sign,
               users.avatar
        from posts
                 inner join users on posts.id = #{id} and users.phone = posts.phone
    </select>

</mapper>